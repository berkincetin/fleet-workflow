apiVersion: apps/v1
kind: Deployment
metadata:
  name: qdrant
  namespace: {{ include "fleet.namespace" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qdrant
  template:
    metadata:
      labels:
        app: qdrant
    spec:
      containers:
        - name: qdrant
          image: {{ .Values.qdrant.image }}
          ports:
            - containerPort: 6333
---
apiVersion: v1
kind: Service
metadata:
  name: qdrant
  namespace: {{ include "fleet.namespace" . }}
spec:
  selector:
    app: qdrant
  ports:
    - port: 6333
      targetPort: 6333
